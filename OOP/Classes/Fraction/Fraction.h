class Fraction {
private:
    int n;
    int d;

public:

    Fraction();
    Fraction(int nominator, int denominator);

    void Displayfraction();
    void Findinversefraction();
    void Modifynominator(int modification);
    void Modifydenominator(int modification);
        
};

